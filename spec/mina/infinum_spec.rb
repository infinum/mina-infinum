require 'spec_helper'

describe Mina::Infinum do
  it 'has a version number' do
    expect(Mina::Infinum::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
